const express = require('express');
const router = express.Router();
//const {  } = require('../controllers/clientController');

//router.route("/clients/getClients").get(client)


module.exports = router;